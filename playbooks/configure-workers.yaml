---

- name: Configuración de los nodos workers
  hosts: "apache"
  gather_facts: true
  vars_files:
    - "group_vars/hosts.yaml"
    - "group_vars/configure-workers.yaml"
  roles:
    - configure-workers